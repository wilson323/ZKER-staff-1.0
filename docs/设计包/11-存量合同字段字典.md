# 存量 JSON Schema 字段字典

生成日期：2026-09-11。来源为当前 ZKER-staff contracts/jsonschema 下全部 JSON Schema。只导出合同字段，不把合同存在视为数据库、接口或运行已实现；物理模型仍需核对当前迁移。新增协作对象见 09-协作扩展候选.schema.json。
 本次共 64 个 Schema 文件、895 个顶层及 $defs 字段条目。
## access-request

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/access-request.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| requestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| requesterId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| roleKey | 是 | string | {"pattern": "^[A-Z][A-Z0-9_]{2,63}$"} |
| scope | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| fieldSet | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| durationSeconds | 是 | integer | {"minimum": 60, "maximum": 2592000} |
| purpose | 是 | string | {"minLength": 10, "maxLength": 500} |
| risk | 是 | enum | {"enum": ["R0", "R1", "R2", "R3", "R4"]} |
| approverSlots | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "object", "additionalProperties": false, "required": ["slotKey", "requiredRoleKey", "status"], "properties": {"slotKey": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "requiredRoleKey": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "assignedNaturalPersonId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "status": {"enum": ["OPEN", "APPROVED", "REJECTED", "EXPIRED"]}}}} |
| status | 是 | enum | {"enum": ["draft", "submitted", "under_review", "approved", "rejected", "cancelled", "expired", "fulfilled", "fulfillment_failed"]} |
| revision | 是 | integer | {"minimum": 1} |

## access-review-campaign

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/access-review-campaign.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| campaignId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| scopeQueryDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| snapshotAt | 是 | string | {"format": "date-time"} |
| reviewerPolicy | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| dueAt | 是 | string | {"format": "date-time"} |
| nonResponsePolicy | 是 | enum | {"enum": ["freeze", "revoke", "escalate"]} |
| status | 是 | enum | {"enum": ["draft", "scheduled", "collecting", "review", "applying", "completed", "completed_with_exceptions", "failed"]} |
| items | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "object", "additionalProperties": false, "required": ["itemId", "bindingId", "bindingRevision", "subjectId", "roleKey", "scope", "risk", "decision"], "properties": {"itemId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "bindingId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "bindingRevision": {"type": "integer", "minimum": 1}, "subjectId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "roleKey": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "scope": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}}, "lastUsedAt": {"type": "string", "format": "date-time"}, "risk": {"enum": ["R0", "R1", "R2", "R3", "R4"]}, "decision": {"enum": ["pending", "retain", "reduce", "revoke", "escalate"]}, "reason": {"type": "string", "maxLength": 500}}}} |
| revision | 是 | integer | {"minimum": 1} |

## approval-receipt

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/approval-receipt.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| receiptId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| approvalRequestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| action | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| resourceId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| effectDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| policyVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| nonce | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["issued", "consumed", "expired", "revoked"]} |
| expiresAt | 是 | string | {"format": "date-time"} |
| consumedAt | 否/条件 | string | {"format": "date-time"} |

## authentication-session

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/authentication-session.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| sessionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| userId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| membershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| authnMethods | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"enum": ["PASSWORD", "PASSKEY", "TOTP", "WEBAUTHN", "IDP", "RECOVERY_CODE"]}} |
| authnStrength | 是 | enum | {"enum": ["AAL1", "AAL2", "AAL3"]} |
| deviceId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| deviceTrust | 是 | enum | {"enum": ["UNKNOWN", "UNMANAGED", "MANAGED", "TRUSTED"]} |
| ipSummary | 否/条件 | string | {"minLength": 3, "maxLength": 64} |
| createdAt | 是 | string | {"format": "date-time"} |
| lastSeenAt | 是 | string | {"format": "date-time"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| authzEpoch | 是 | integer | {"minimum": 0} |
| status | 是 | enum | {"enum": ["active", "step_up_required", "revoked", "expired", "risk_locked"]} |
| revision | 是 | integer | {"minimum": 1} |

## authorization-decision

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/authorization-decision.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| decisionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| action | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| resourceId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| effect | 是 | enum | {"enum": ["ALLOW", "DENY"]} |
| policyId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| policyVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| authzEpoch | 是 | integer | {"minimum": 0} |
| obligations | 否/条件 | array | {"items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| reasonCodes | 否/条件 | array | {"items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| decidedAt | 是 | string | {"format": "date-time"} |

## authorization-grant

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/authorization-grant.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| grantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| actions | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$"}} |
| resourceScope | 是 | array | {"minItems": 1, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| sourceType | 是 | enum | {"enum": ["PLATFORM_ROLE", "RESOURCE_ROLE", "DIRECT", "DELEGATION", "BREAK_GLASS"]} |
| status | 是 | enum | {"enum": ["active", "revoked", "expired"]} |
| validFrom | 是 | string | {"format": "date-time"} |
| expiresAt | 否/条件 | string | {"format": "date-time"} |
| revision | 否/条件 | integer | {"minimum": 1} |

## break-glass-session

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/break-glass-session.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| sessionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| incidentId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| userId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| actions | 是 | array | {"minItems": 1, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| resourceScope | 是 | array | {"minItems": 1, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| status | 是 | enum | {"enum": ["requested", "active", "revoked", "expired", "closed"]} |
| activatedAt | 是 | string | {"format": "date-time"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| approverNaturalPersonIds | 是 | array | {"minItems": 2, "uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| reviewerNaturalPersonId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |

## budget-policy

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/budget-policy.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| budgetPolicyId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| scopeType | 是 | enum | {"enum": ["TENANT", "TEAM", "AGENT", "WORK_EXECUTION"]} |
| scopeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| period | 是 | enum | {"enum": ["DAY", "WEEK", "MONTH", "EXECUTION"]} |
| budgetAmount | 是 | number | {} |
| thresholdMode | 是 | enum | {"enum": ["PERCENT_OF_BUDGET", "ABSOLUTE_AMOUNT"]} |
| softThreshold | 是 | number | {"minimum": 0} |
| hardThreshold | 是 | number | {} |
| forecastWindowDays | 是 | integer | {"minimum": 1, "maximum": 366} |
| currency | 是 | string | {"pattern": "^[A-Z]{3}$"} |
| allowDegrade | 是 | boolean | {} |
| approverRole | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "active", "superseded", "archived"]} |
| revision | 是 | integer | {"minimum": 1} |

## capability-manifest

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/capability-manifest.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| capabilityId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| version | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| kind | 是 | enum | {"enum": ["SKILL", "TOOL", "MCP", "MODEL", "KNOWLEDGE"]} |
| riskLevel | 是 | enum | {"enum": ["R0", "R1", "R2", "R3", "R4"]} |
| status | 是 | enum | {"enum": ["draft", "reviewed", "enabled", "disabled", "archived"]} |
| inputSchemaDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| outputSchemaDigest | 否/条件 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| networkPolicyId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| compensationPolicyId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |

## command-candidate

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/command-candidate.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| candidateId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| operationId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| action | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| resourceType | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| resourceId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| riskLevel | 是 | enum | {"enum": ["R0", "R1", "R2", "R3", "R4"]} |
| impactPreview | 否/条件 | object | {} |
| requiresReauth | 是 | boolean | {} |
| requiresApproval | 是 | boolean | {} |
| expiresAt | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## completion-check-result

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/completion-check-result.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| completionCheckResultId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| executionAttemptId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| rulesRevision | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| expectedWorkItemRevision | 是 | integer | {"minimum": 1} |
| inputResultDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| checkResultsDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| evidenceManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| outcome | 是 | enum | {"enum": ["passed", "failed", "blocked"]} |
| evaluatedBySubjectType | 是 | enum | {"enum": ["HUMAN", "POLICY_SERVICE"]} |
| evaluatedBySubjectId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| evaluatedAt | 是 | string | {"format": "date-time"} |
| failedCheckKeys | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| revision | 否/条件 | integer | {"minimum": 1} |

## context-manifest

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/context-manifest.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| contextManifestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceWorkItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetNodeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetSubjectType | 是 | enum | {"enum": ["HUMAN", "AGENT", "ROLE", "UNRESOLVED"]} |
| targetSubjectId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| purpose | 是 | string | {"minLength": 1} |
| sourceRefsDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| fieldProjectionDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| authzEpoch | 是 | integer | {"minimum": 1} |
| policyRevision | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| createdAt | 是 | string | {"format": "date-time"} |

## data-deletion-request

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/data-deletion-request.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| requestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| requestType | 是 | enum | {"enum": ["policy_expiry", "DSAR", "tenant_exit", "policy_change"]} |
| status | 是 | enum | {"enum": ["draft", "cancelled", "scope_frozen", "rejected", "hold_evaluated", "blocked_by_hold", "approval_required", "approved", "queued", "in_progress", "partial_deletion", "reconciling", "completed"]} |
| scopeFrozen | 是 | object | {} |
| targetCount | 是 | integer | {"minimum": 1} |
| heldExactHoldRefs | 否/条件 | array | {"items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| approvalReceiptRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| createdByNaturalPersonId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| executorSubjectType | 否/条件 | enum | {"enum": ["SYSTEM_JOB", "SERVICE_ACCOUNT"]} |
| revision | 是 | integer | {"minimum": 1} |

## delegation

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/delegation.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| delegationId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| delegatorId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| delegateeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| actionSet | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| resourceScope | 是 | array | {"minItems": 1, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| sourceRef | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceGrantRevisions | 否/条件 | array | {"minItems": 1, "items": {"type": "integer", "minimum": 1}} |
| validFrom | 是 | string | {"format": "date-time"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["proposed", "active", "suspended", "revoked", "rejected", "cancelled", "expired"]} |
| revision | 是 | integer | {"minimum": 1} |
| redelegationAllowed | 否/条件 | 组合定义 | {"const": false} |

## deletion-ledger-entry

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/deletion-ledger-entry.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| entryId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| entryType | 是 | enum | {"enum": ["delete", "crypto_erase", "tombstone", "hold_apply", "hold_release", "hold_revoke", "key_revoke", "downstream_receipt", "beyond_use"]} |
| objectRef | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| occurredAt | 是 | string | {"format": "date-time"} |
| actorSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| relatedHoldId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| relatedDeletionRequestId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| digest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| revision | 是 | integer | {"minimum": 1} |

## deletion-target

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/deletion-target.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| targetId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| deletionRequestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetClass | 是 | enum | {"enum": ["live_payload", "dsh_session_artifact", "spill", "artifact_or_derived", "search_or_vector", "cache_queue_dlq", "otel", "export", "external_processor", "backup"]} |
| status | 是 | enum | {"enum": ["pending", "dispatched", "deleted", "crypto_erased", "tombstoned", "beyond_use", "not_applicable_with_reason", "held_with_exact_hold_ref", "failed"]} |
| idempotencyKey | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| receiptRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| errorCode | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| retryAfter | 否/条件 | string | {"format": "date-time"} |
| exactHoldRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| revision | 是 | integer | {"minimum": 1} |

## duty-transfer-item

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/duty-transfer-item.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| itemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| dutyType | 是 | enum | {"enum": ["WORK_ITEM", "APPROVAL_SLOT", "TEAM_LEADER", "ON_CALL", "PROCESS_RESPONSIBLE"]} |
| dutyId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| fromMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| toMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| effectiveAt | 否/条件 | string | {"format": "date-time"} |
| acceptedAt | 否/条件 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["pending", "accepted", "transferred", "blocked", "reconcile_required"]} |
| revision | 是 | integer | {"minimum": 1} |

## employment-assignment

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/employment-assignment.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| assignmentId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| membershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| positionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| assignmentType | 是 | enum | {"enum": ["PRIMARY", "SECONDARY"]} |
| allocationPercent | 是 | integer | {"minimum": 1, "maximum": 100} |
| validFrom | 是 | string | {"format": "date-time"} |
| validUntil | 否/条件 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["planned", "active", "ended", "cancelled"]} |
| revision | 是 | integer | {"minimum": 1} |

## evaluation-report

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/evaluation-report.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| reportId | 是 | string | {"minLength": 1} |
| tenantId | 是 | string | {"minLength": 1} |
| proposalId | 是 | string | {"minLength": 1} |
| datasetDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| baselineDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| candidateDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| runtimeProfileRef | 是 | string | {"minLength": 1} |
| dshCommit | 是 | string | {"pattern": "^[0-9a-f]{40}$"} |
| trialCount | 是 | integer | {"minimum": 3} |
| blindReview | 是 | boolean | {} |
| evaluatorNaturalPersonId | 是 | string | {"minLength": 1} |
| scores | 是 | object | {} |
| hardGates | 是 | object | {} |
| status | 是 | string | {"enum": ["RUNNING", "FAILED", "BLOCKED", "PASSED"]} |
| createdAt | 是 | string | {"format": "date-time"} |

## evolution-proposal

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/evolution-proposal.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| proposalId | 是 | string | {"minLength": 1} |
| tenantId | 是 | string | {"minLength": 1} |
| targetType | 是 | string | {"enum": ["AGENT", "SOP", "SKILL", "RUNTIME_PROFILE"]} |
| targetId | 是 | string | {"minLength": 1} |
| baselineDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| candidateDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| sourceManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| riskLevel | 是 | string | {"enum": ["R0", "R1", "R2", "R3", "R4"]} |
| proposerNaturalPersonId | 是 | string | {"minLength": 1} |
| expectedRevision | 是 | integer | {"minimum": 0} |
| status | 是 | string | {"enum": ["PROPOSED", "EVIDENCE_READY", "EVALUATING", "EVALUATION_FAILED", "READY_FOR_APPROVAL", "APPROVED", "REJECTED", "CANARY", "PUBLISHED", "ROLLED_BACK", "SUPERSEDED"]} |
| createdAt | 是 | string | {"format": "date-time"} |

## execution-envelope

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/execution-envelope.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| envelopeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| attemptId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| snapshotDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| nonce | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| fencingToken | 是 | integer | {"minimum": 1} |
| issuedAt | 是 | string | {"format": "date-time"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| signature | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |

## execution-snapshot

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/execution-snapshot.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| snapshotId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workExecutionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| attemptId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sopVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| agentDefinitionVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| runtimeProfileVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| policyVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| authzEpoch | 是 | integer | {"minimum": 0} |
| capabilities | 是 | array | {"items": {"$ref": "capability-manifest.schema.json"}} |
| digest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |

## global-search

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/global-search.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| query | 是 | string | {"minLength": 1, "maxLength": 512} |
| types | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"enum": ["WORK_EXECUTION", "WORK_ITEM", "SOP", "CONVERSATION", "AGENT", "KNOWLEDGE", "SKILL", "TOOL", "TEAM", "ORGANIZATION"]}} |
| statusFilters | 否/条件 | array | {"items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| ownerId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| dateFrom | 否/条件 | string | {"format": "date-time"} |
| dateTo | 否/条件 | string | {"format": "date-time"} |
| scope | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| cursor | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| limit | 否/条件 | integer | {"minimum": 1, "maximum": 100} |

## group-membership

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/group-membership.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| groupMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| groupId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| membershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceType | 是 | enum | {"enum": ["MANUAL", "DYNAMIC_RULE", "SCIM"]} |
| sourceRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| validFrom | 是 | string | {"format": "date-time"} |
| validUntil | 否/条件 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["active", "expired", "revoked"]} |
| revision | 是 | integer | {"minimum": 1} |

## group

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/group.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| groupId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| name | 是 | string | {"minLength": 1, "maxLength": 120} |
| type | 是 | enum | {"enum": ["STATIC", "DYNAMIC"]} |
| membershipRuleRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "active", "suspended", "closed"]} |
| revision | 是 | integer | {"minimum": 1} |

## handoff-package

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/handoff-package.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| handoffPackageId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceWorkItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceNodeRunId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetNodeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetWorkItemId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| packageVersion | 是 | integer | {"minimum": 1} |
| threadDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| resultDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| completionCheckResultId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| completionCheckDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| evidenceManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| unresolvedItemsDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| contextManifestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| contextManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| traceRefDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| submittedByMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| submittedAt | 是 | string | {"format": "date-time"} |
| kind | 否/条件 | enum | {"enum": ["NORMAL_NODE_HANDOFF"]} |

## identity-provider

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/identity-provider.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| providerId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| type | 是 | enum | {"enum": ["OIDC", "SAML"]} |
| issuer | 是 | string | {"format": "uri", "minLength": 8} |
| audience | 是 | string | {"minLength": 1, "maxLength": 256} |
| metadataDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| certificateRefs | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| status | 是 | enum | {"enum": ["draft", "testing", "enabled", "disabled", "error"]} |
| revision | 是 | integer | {"minimum": 1} |

## invitation

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/invitation.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| invitationId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| inviteeEmailMasked | 是 | string | {"minLength": 3, "maxLength": 254} |
| targetOrgUnitId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetPositionId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| expiresAt | 是 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["issued", "accepted", "expired", "cancelled"]} |
| issuedBy | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| acceptedMembershipId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| revision | 是 | integer | {"minimum": 1} |

## legal-entity

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/legal-entity.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| legalEntityId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| code | 是 | string | {"pattern": "^[A-Z0-9][A-Z0-9_-]{1,31}$"} |
| name | 是 | string | {"minLength": 1, "maxLength": 160} |
| countryCode | 是 | string | {"pattern": "^[A-Z]{2}$"} |
| status | 是 | enum | {"enum": ["draft", "active", "closing", "closed"]} |
| effectiveFrom | 是 | string | {"format": "date-time"} |
| effectiveTo | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## legal-hold

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/legal-hold.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| holdId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft_request", "scope_frozen", "pending_approval", "active", "review_due", "renewed", "release_pending", "released", "revoked"]} |
| caseOrIncidentRef | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| legalBasisRef | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| purpose | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectLayers | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"enum": ["EventEnvelope", "PromptModelPayload", "ToolPayload", "ArtifactRetentionRef", "SpillObjectManifest"]}} |
| explicitObjectRefs | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| frozenQueryRevision | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| hitCount | 是 | integer | {"minimum": 1} |
| classification | 是 | enum | {"enum": ["C0", "C1", "C2", "C3", "C4"]} |
| timeRange | 否/条件 | object | {} |
| createdByNaturalPersonId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| approverSlots | 否/条件 | array | {"minItems": 2, "items": {"type": "object", "additionalProperties": false, "required": ["slotRole", "naturalPersonId"], "properties": {"slotRole": {"enum": ["SECURITY_REVIEWER", "PRIVACY_OFFICER", "SECURITY_ADMIN"]}, "naturalPersonId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "decision": {"enum": ["pending", "approved", "rejected"]}, "decidedAt": {"type": "string", "format": "date-time"}}}} |
| reviewAt | 是 | string | {"format": "date-time"} |
| accessAllowlistRevision | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| originalDeleteAfterPreserved | 否/条件 | 组合定义 | {"const": true} |
| wildcardTenantForbidden | 否/条件 | 组合定义 | {"const": true} |
| revision | 是 | integer | {"minimum": 1} |

## lifecycle-audit-event

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/lifecycle-audit-event.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| eventId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| aggregateType | 是 | enum | {"enum": ["TENANT", "LEGAL_ENTITY", "ORG_UNIT", "POSITION", "ASSIGNMENT", "USER", "MEMBERSHIP", "GROUP", "ROLE_BINDING", "OFFBOARDING_PLAN"]} |
| aggregateId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| eventType | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| actorSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| decisionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| beforeDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| afterDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| revision | 是 | integer | {"minimum": 1} |
| occurredAt | 是 | string | {"format": "date-time"} |
| evidenceRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |

## node-contract

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/node-contract.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| nodeKey | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| responsibleRole | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| reviewerRole | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| approverRole | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| executionMode | 是 | enum | {"enum": ["HUMAN", "AGENT", "HYBRID"]} |
| candidateRoleKeys | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| capabilityPolicyIds | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| inputSchemaRef | 是 | string | {"format": "uri-reference"} |
| outputSchemaRef | 是 | string | {"format": "uri-reference"} |
| failurePolicy | 是 | object | {} |

## notification-preference

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/notification-preference.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| preferenceId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| categoryRules | 是 | object | {} |
| timezone | 是 | string | {"pattern": "^(?:UTC\|[A-Za-z_]+(?:/[A-Za-z0-9._+-]+)+)$"} |
| quietHours | 否/条件 | object | {} |
| digestMode | 否/条件 | enum | {"enum": ["OFF", "DAILY", "WEEKLY"]} |
| mandatorySecurityEnabled | 是 | 组合定义 | {"const": true} |
| revision | 是 | integer | {"minimum": 1} |

## notification

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/notification.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| notificationId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| recipientSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| eventType | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectType | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| titleKey | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| bodyKey | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| redactedParams | 否/条件 | object | {} |
| priority | 是 | enum | {"enum": ["LOW", "MEDIUM", "HIGH", "URGENT"]} |
| actionRequired | 否/条件 | boolean | {} |
| allowedChannels | 否/条件 | array | {"uniqueItems": true, "items": {"enum": ["IN_APP", "EMAIL", "IM"]}} |
| deepLink | 否/条件 | string | {} |
| dedupeKey | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| createdAt | 是 | string | {"format": "date-time"} |
| expiresAt | 否/条件 | string | {"format": "date-time"} |
| readAt | 否/条件 | string | {"format": "date-time"} |
| acknowledgedAt | 否/条件 | string | {"format": "date-time"} |
| snoozedUntil | 否/条件 | string | {"format": "date-time"} |
| archivedAt | 否/条件 | string | {"format": "date-time"} |
| escalationPolicyId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| evidenceId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["unread", "read", "acknowledged", "snoozed", "archived", "expired"]} |
| revision | 是 | integer | {"minimum": 1} |

## offboarding-command

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/offboarding-command.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| commandId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| userId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| expectedMembershipRevision | 是 | integer | {"minimum": 1} |
| transferPlanId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| requestedBy | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| requestedAt | 是 | string | {"format": "date-time"} |
| revokeSessions | 否/条件 | 组合定义 | {"const": true} |
| revokeDelegations | 否/条件 | 组合定义 | {"const": true} |

## offboarding-plan

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/offboarding-plan.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| planId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| membershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| snapshotRevision | 是 | integer | {"minimum": 1} |
| ownershipItems | 是 | array | {"items": {"$ref": "ownership-transfer-item.schema.json"}} |
| dutyItems | 是 | array | {"items": {"$ref": "duty-transfer-item.schema.json"}} |
| sessionIds | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| roleBindingIds | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| delegationIds | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| credentialRefs | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| externalDeprovisionSteps | 是 | array | {"items": {"type": "object", "required": ["system", "effectKey", "status"], "additionalProperties": false, "properties": {"system": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "effectKey": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "status": {"enum": ["pending", "succeeded", "failed", "result_unknown", "reconciled"]}, "receiptRef": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "reconcilerId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}}}} |
| blockingReasons | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| exceptionSignoffBy | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "ready", "executing", "reconciling", "completed", "completed_with_exceptions", "cancelled"]} |
| revision | 是 | integer | {"minimum": 1} |

## organization-unit

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/organization-unit.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| orgUnitId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| legalEntityId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| parentOrgUnitId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| code | 是 | string | {"pattern": "^[A-Z0-9][A-Z0-9_-]{1,31}$"} |
| name | 是 | string | {"minLength": 1, "maxLength": 120} |
| type | 是 | enum | {"enum": ["COMPANY", "DIVISION", "DEPARTMENT", "TEAM", "COST_CENTER", "LOCATION"]} |
| status | 是 | enum | {"enum": ["draft", "active", "reorganizing", "closing", "closed"]} |
| effectiveFrom | 是 | string | {"format": "date-time"} |
| effectiveTo | 否/条件 | string | {"format": "date-time"} |
| managerPositionId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| costCenterRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| locationRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| revision | 是 | integer | {"minimum": 1} |

组合约束 顶层.allOf（以源合同完整定义为准）：

~~~json
[
  {
    "if": {
      "properties": {
        "status": {
          "const": "closed"
        }
      },
      "required": [
        "status"
      ]
    },
    "then": {
      "required": [
        "effectiveTo"
      ]
    }
  }
]
~~~

## ownership-transfer-item

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/ownership-transfer-item.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| itemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectType | 是 | enum | {"enum": ["AGENT", "SOP", "KNOWLEDGE", "SKILL", "TOOL", "SECRET_REF", "TEAM"]} |
| objectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| fromMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| toMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| requiredConfirmation | 否/条件 | boolean | {} |
| confirmedBy | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| confirmedAt | 否/条件 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["pending", "accepted", "rejected", "transferred", "reconcile_required"]} |
| revision | 是 | integer | {"minimum": 1} |

## platform-role

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/platform-role.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| roleKey | 是 | string | {"pattern": "^[A-Z][A-Z0-9_]{2,63}$"} |
| name | 是 | string | {"minLength": 1, "maxLength": 120} |
| kind | 是 | enum | {"enum": ["PLATFORM", "RESOURCE"]} |
| allowedActionIds | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$"}} |
| maxRisk | 是 | enum | {"enum": ["R0", "R1", "R2", "R3", "R4"]} |
| maxDataClass | 是 | enum | {"enum": ["C0", "C1", "C2", "C3", "C4"]} |
| delegableActions | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$"}} |
| status | 是 | enum | {"enum": ["draft", "published", "deprecated", "revoked"]} |
| version | 是 | integer | {"minimum": 1} |
| revision | 是 | integer | {"minimum": 1} |

## position

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/position.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| positionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| orgUnitId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| title | 是 | string | {"minLength": 1, "maxLength": 120} |
| jobCode | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| managerPositionId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "active", "vacant", "closing", "closed"]} |
| capacity | 是 | integer | {"minimum": 1, "maximum": 100} |
| effectiveFrom | 是 | string | {"format": "date-time"} |
| effectiveTo | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## provisioning-connection

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/provisioning-connection.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| connectionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| providerId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| type | 是 | enum | {"enum": ["SCIM", "HRIS", "IDP_GROUP_SYNC"]} |
| sourceSystem | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| attributeMappingVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| groupMappingVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| conflictPolicy | 是 | enum | {"enum": ["MANUAL_RECONCILIATION", "SOURCE_WINS_NON_SECURITY_FIELDS", "REJECT_BATCH"]} |
| status | 是 | enum | {"enum": ["draft", "dry_run_ready", "enabled", "paused", "error", "disabled"]} |
| lastCursor | 否/条件 | string | {"maxLength": 512} |
| lastSuccessfulRunAt | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## quota-increase-decision

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/quota-increase-decision.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| requestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| decision | 是 | enum | {"enum": ["APPROVE", "REJECT"]} |
| decisionReason | 是 | string | {"minLength": 1, "maxLength": 2000} |
| approverSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| expectedRevision | 是 | integer | {"minimum": 1} |
| authnContext | 否/条件 | enum | {"enum": ["MFA_STRONG", "WEBAUTHN"]} |

## quota-increase-request

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/quota-increase-request.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| requestId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| scopeType | 是 | enum | {"enum": ["TENANT", "TEAM", "AGENT", "WORK_EXECUTION"]} |
| scopeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| quotaType | 是 | enum | {"enum": ["CONCURRENCY", "RUN", "TOKEN", "TOOL_CALL", "STORAGE", "EGRESS", "RATE"]} |
| requestedLimit | 是 | number | {} |
| validUntil | 是 | string | {"format": "date-time"} |
| reason | 是 | string | {"minLength": 1} |
| costImpact | 是 | number | {"minimum": 0} |
| requesterSubjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| decisionReceiptId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["requested", "approved", "rejected", "expired", "cancelled"]} |
| revision | 是 | integer | {"minimum": 1} |

## quota

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/quota.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| quotaId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| scopeType | 是 | enum | {"enum": ["TENANT", "TEAM", "AGENT", "WORK_EXECUTION"]} |
| scopeId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| quotaType | 是 | enum | {"enum": ["CONCURRENCY", "RUN", "TOKEN", "TOOL_CALL", "STORAGE", "EGRESS", "RATE"]} |
| limit | 是 | number | {"minimum": 0} |
| used | 是 | number | {"minimum": 0} |
| periodStart | 是 | string | {"format": "date-time"} |
| periodEnd | 是 | string | {"format": "date-time"} |
| status | 是 | enum | {"enum": ["available", "warning", "exhausted", "suspended"]} |
| revision | 是 | integer | {"minimum": 1} |

## release-candidate

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/release-candidate.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| releaseCandidateId | 是 | string | {"minLength": 1} |
| tenantId | 是 | string | {"minLength": 1} |
| proposalId | 是 | string | {"minLength": 1} |
| evaluationReportId | 是 | string | {"minLength": 1} |
| approvalReceiptId | 是 | string | {"minLength": 1} |
| candidateDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| evaluationDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| publisherNaturalPersonId | 是 | string | {"minLength": 1} |
| canaryPolicy | 是 | object | {} |
| rollbackPointer | 是 | string | {"minLength": 1} |
| expectedRevision | 是 | integer | {"minimum": 0} |
| status | 是 | string | {"enum": ["APPROVED", "CANARY", "PUBLISHED", "ROLLED_BACK", "FAILED"]} |
| createdAt | 是 | string | {"format": "date-time"} |

## reorganization-plan

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/reorganization-plan.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| planId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sourceOrgUnitId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| targetParentOrgUnitId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| effectiveAt | 是 | string | {"format": "date-time"} |
| impactSnapshotDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| affectedMembershipIds | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| affectedPositionIds | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| affectedBindingIds | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| affectedWorkItemIds | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| status | 是 | enum | {"enum": ["draft", "validated", "scheduled", "executing", "reconciling", "completed", "cancelled", "failed"]} |
| revision | 是 | integer | {"minimum": 1} |

## restore-deletion-replay-receipt

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/restore-deletion-replay-receipt.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| receiptId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| restoreJobId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["quarantined", "replaying", "scanned", "blocked", "signed_off", "traffic_opened"]} |
| ledgerWatermark | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| backupManifestDigest | 否/条件 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| replayedEntryCount | 是 | integer | {"minimum": 0} |
| canaryScanStatus | 是 | enum | {"enum": ["pending", "clean", "resurrection_detected", "cross_tenant_detected"]} |
| auditorSignoffNaturalPersonId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| restoreExecutorNaturalPersonId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| failClosedReason | 否/条件 | enum | {"enum": ["LEDGER_MISSING", "WATERMARK_REGRESSED", "KEY_REVOKED", "CANARY_RESURRECTED", "CROSS_TENANT_REF", "AUDITOR_SIGNOFF_MISSING"]} |
| revision | 是 | integer | {"minimum": 1} |

## retention-impact-preview

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/retention-impact-preview.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| previewId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| policyRevisionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["computing", "ready", "stale", "unavailable", "conflict"]} |
| frozenRevision | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| hitCount | 是 | integer | {"minimum": 0} |
| changeCount | 是 | integer | {"minimum": 0} |
| earlyDeleteCount | 否/条件 | integer | {"minimum": 0} |
| extendCount | 否/条件 | integer | {"minimum": 0} |
| heldCount | 否/条件 | integer | {"minimum": 0} |
| conflictCount | 否/条件 | integer | {"minimum": 0} |
| costEstimateRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| nonRelaxableFlags | 否/条件 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| computedAt | 是 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## retention-policy-revision

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/retention-policy-revision.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| revisionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "previewed", "pending_approval", "approved", "scheduled", "effective", "superseded", "rejected", "cancelled"]} |
| layers | 是 | array | {"minItems": 5, "maxItems": 5, "items": {"type": "object", "additionalProperties": false, "required": ["objectLayer", "purpose", "classification", "clockBasis", "valueStatus"], "properties": {"objectLayer": {"enum": ["EventEnvelope", "PromptModelPayload", "ToolPayload", "ArtifactRetentionRef", "SpillObjectManifest"]}, "purpose": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "classification": {"enum": ["C0", "C1", "C2", "C3", "C4"]}, "clockBasis": {"enum": ["object_created_at", "last_access_at", "session_closed_at", "policy_effective_at", "evidence_class_start"]}, "valueStatus": {"enum": ["CANDIDATE", "EVIDENCE_GATED", "APPROVED"]}, "selectedValueRef": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "evidenceRefs": {"type": "array", "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "uniqueItems": true}, "exceptionRulesRef": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "reviewAt": {"type": "string", "format": "date-time"}}, "allOf": [{"if": {"properties": {"valueStatus": {"const": "APPROVED"}}, "required": ["valueStatus"]}, "then": {"required": ["selectedValueRef", "evidenceRefs"], "properties": {"evidenceRefs": {"minItems": 1}}}}]}} |
| authorNaturalPersonId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| publisherNaturalPersonId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| securityReviewerNaturalPersonId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| impactPreviewId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| approvalReceiptRefs | 否/条件 | array | {"items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| effectiveFrom | 否/条件 | string | {"format": "date-time"} |
| createdAt | 是 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |
| immutableOnceEffective | 否/条件 | 组合定义 | {"const": true} |

## role-binding

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/role-binding.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| bindingId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectType | 是 | enum | {"enum": ["HUMAN_USER", "TENANT_MEMBERSHIP", "GROUP"]} |
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| roleKey | 是 | string | {"pattern": "^[A-Z][A-Z0-9_]{2,63}$"} |
| resourceScope | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| conditionRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| fieldPolicyRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| validFrom | 是 | string | {"format": "date-time"} |
| expiresAt | 否/条件 | string | {"format": "date-time"} |
| sourceType | 是 | enum | {"enum": ["DIRECT_ADMIN", "ACCESS_REQUEST", "GROUP_MAPPING", "DELEGATION", "BREAK_GLASS"]} |
| sourceRef | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["draft", "requested", "approved", "active", "suspended", "revoked", "expired"]} |
| revision | 是 | integer | {"minimum": 1} |

## search-result

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/search-result.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| resultId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectType | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| objectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| title | 是 | string | {"minLength": 1} |
| status | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| scope | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| updatedAt | 是 | string | {"format": "date-time"} |
| allowedActions | 是 | array | {"uniqueItems": true, "items": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}} |
| dataTime | 否/条件 | string | {"format": "date-time"} |
| redacted | 否/条件 | boolean | {} |

## sop-ai-candidate

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/sop-ai-candidate.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| candidateId | 是 | string | {"minLength": 1} |
| tenantId | 是 | string | {"minLength": 1} |
| baseSopVersionId | 是 | string | {"minLength": 1} |
| baseRevision | 是 | integer | {"minimum": 0} |
| intent | 是 | string | {"minLength": 1} |
| immutableConstraints | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}} |
| sourceManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| promptDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| modelRef | 是 | string | {"minLength": 1} |
| runtimeProfileRef | 是 | string | {"minLength": 1} |
| dshCommit | 是 | string | {"pattern": "^[0-9a-f]{40}$"} |
| candidateDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| hunks | 是 | array | {"minItems": 1, "items": {"type": "object", "additionalProperties": false, "required": ["hunkId", "kind", "sourceRefs", "riskLevel", "decision"], "properties": {"hunkId": {"type": "string", "minLength": 1}, "kind": {"type": "string", "enum": ["ADD", "UPDATE", "REMOVE", "REWIRE"]}, "sourceRefs": {"type": "array", "minItems": 1, "items": {"type": "string", "minLength": 1}}, "riskLevel": {"type": "string", "enum": ["R0", "R1", "R2", "R3", "R4"]}, "decision": {"type": "string", "enum": ["PENDING", "ACCEPTED", "REJECTED"]}}}} |
| status | 是 | string | {"enum": ["QUEUED", "GENERATING", "PARTIAL", "FAILED", "PENDING_REVIEW", "PENDING_VALIDATION", "REJECTED", "MERGED"]} |
| createdByNaturalPersonId | 是 | string | {"minLength": 1} |
| createdAt | 是 | string | {"format": "date-time"} |

## sop-definition

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/sop-definition.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| sopId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| version | 是 | integer | {"minimum": 1} |
| status | 是 | enum | {"enum": ["draft", "validated", "published", "archived"]} |
| nodes | 是 | array | {"minItems": 1, "items": {"$ref": "node-contract.schema.json"}} |
| edges | 是 | array | {"items": {"type": "object", "required": ["from", "to"], "properties": {"from": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}, "to": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}}, "additionalProperties": false}} |
| publishedDigest | 否/条件 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |

## sop-version-history-item

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/sop-version-history-item.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| sopVersionHistoryItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sopId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sopRevision | 是 | integer | {"minimum": 1} |
| status | 是 | enum | {"enum": ["draft", "validated", "published", "archived", "rolled_back"]} |
| publishedDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| changeSummary | 否/条件 | string | {} |
| changedAt | 是 | string | {"format": "date-time"} |
| changedByMembershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| publisherDistinctFromDesigner | 否/条件 | boolean | {} |

## staffdeck-agent-profile

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/staffdeck-agent-profile.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| id | 是 | string | {"minLength": 1} |
| tenant_id | 是 | string | {"minLength": 1} |
| name | 是 | string | {"minLength": 1} |
| description | 否/条件 | ['string', 'null'] | {} |
| persona_prompt | 否/条件 | ['string', 'null'] | {} |
| is_overall | 是 | boolean | {} |
| status | 是 | string | {} |
| harness_max_actions | 是 | integer | {"minimum": 1, "maximum": 100} |
| metadata | 是 | #/$defs/metadata | {} |
| resources | 是 | array | {"items": {"$ref": "#/$defs/resourceBinding"}} |
| access | 否/条件 | 组合定义 | {"anyOf": [{"$ref": "#/$defs/access"}, {"type": "null"}]} |
| created_at | 是 | string | {"minLength": 1} |
| updated_at | 是 | string | {"minLength": 1} |

### metadata

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| role_name | 否/条件 | string | {} |
| onboarded_at | 否/条件 | string | {} |
| system_prompt_summary | 否/条件 | string | {} |
| work_styles | 否/条件 | #/$defs/stringList | {} |
| expertise_tags | 否/条件 | #/$defs/stringList | {} |
| work_modes | 否/条件 | #/$defs/stringList | {} |
| published_to_gallery | 否/条件 | boolean | {} |
| avatar_kind | 否/条件 | enum | {"enum": ["preset", "upload"]} |
| avatar_preset | 否/条件 | string | {} |
| avatar_text | 否/条件 | string | {} |
| avatar_tone | 否/条件 | string | {} |
| avatar_image | 否/条件 | string | {} |

### resourceBinding

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| id | 是 | string | {"minLength": 1} |
| tenant_id | 是 | string | {"minLength": 1} |
| agent_id | 是 | string | {"minLength": 1} |
| resource_type | 是 | enum | {"enum": ["skill", "general_skill", "knowledge_base", "tool"]} |
| resource_id | 是 | string | {"minLength": 1} |
| status | 是 | string | {} |
| metadata | 是 | object | {} |
| created_at | 是 | string | {"minLength": 1} |
| updated_at | 是 | string | {"minLength": 1} |

### accessSource

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| source_type | 是 | string | {} |
| source_id | 是 | string | {} |
| access_role | 是 | string | {} |

### access

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| relationship_role | 否/条件 | ['string', 'null'] | {} |
| sources | 是 | array | {"items": {"$ref": "#/$defs/accessSource"}} |
| source_types | 是 | #/$defs/stringList | {} |
| source_count | 是 | integer | {"minimum": 0} |
| can_view | 是 | boolean | {} |
| can_chat | 是 | boolean | {} |
| can_manage | 是 | boolean | {} |
| owned_by_current_user | 是 | boolean | {} |
| capability_source | 是 | string | {} |

### profileCreate

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| name | 否/条件 | ['string', 'null'] | {} |
| description | 否/条件 | ['string', 'null'] | {} |
| persona_prompt | 否/条件 | ['string', 'null'] | {} |
| is_overall | 否/条件 | boolean | {"default": false} |
| source_mode | 否/条件 | enum | {"enum": ["copy", "blank"], "default": "copy"} |
| copy_from_agent_id | 否/条件 | ['string', 'null'] | {} |
| harness_max_actions | 否/条件 | integer | {"minimum": 1, "maximum": 100, "default": 32} |
| metadata | 否/条件 | object | {} |

### profileUpdate

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| name | 否/条件 | ['string', 'null'] | {} |
| description | 否/条件 | ['string', 'null'] | {} |
| persona_prompt | 否/条件 | ['string', 'null'] | {} |
| status | 否/条件 | 组合定义 | {"anyOf": [{"enum": ["active", "archived"]}, {"type": "null"}]} |
| harness_max_actions | 否/条件 | ['integer', 'null'] | {"minimum": 1, "maximum": 100} |
| metadata | 否/条件 | ['object', 'null'] | {} |

### scope

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| agents | 是 | array | {"items": {"type": "object"}} |

### workRecordReplyStats

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| total | 是 | integer | {"minimum": 0} |
| today | 是 | integer | {"minimum": 0} |
| by_day | 是 | object | {} |

### workRecordEvent

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| id | 是 | string | {"minLength": 1} |
| kind | 是 | enum | {"enum": ["chat", "task", "sop", "tool", "knowledge", "skill"]} |
| phase | 是 | enum | {"enum": ["reply", "last_run", "next_run", "assigned"]} |
| timestamp | 是 | string | {"minLength": 1} |
| label | 是 | string | {} |

### workRecord

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| agent_id | 是 | string | {"minLength": 1} |
| timezone | 是 | string | {"minLength": 1} |
| generated_at | 是 | string | {"minLength": 1} |
| reply_stats | 是 | #/$defs/workRecordReplyStats | {} |
| events | 是 | array | {"items": {"$ref": "#/$defs/workRecordEvent"}} |

### resourceBindingInput

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| resource_type | 是 | enum | {"enum": ["skill", "general_skill", "knowledge_base", "tool"]} |
| resource_id | 是 | string | {"minLength": 1} |
| status | 否/条件 | enum | {"enum": ["active", "inactive"], "default": "active"} |
| metadata | 否/条件 | object | {} |

### resourcesUpdate

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| resources | 否/条件 | array | {"items": {"$ref": "#/$defs/resourceBindingInput"}} |

### resourceImport

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| source_agent_id | 是 | string | {"minLength": 1} |
| resource_type | 是 | enum | {"enum": ["skill", "general_skill", "knowledge_base", "tool"]} |
| resource_ids | 否/条件 | array | {"items": {"type": "string", "minLength": 1}} |

### credentialCreate

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| name | 是 | string | {"minLength": 1, "maxLength": 120} |
| access | 否/条件 | 组合定义 | {"const": "runtime", "default": "runtime"} |
| expires_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |

### credentialRead

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| id | 是 | string | {"minLength": 1} |
| agent_id | 是 | string | {"minLength": 1} |
| name | 是 | string | {"minLength": 1} |
| access | 是 | enum | {"enum": ["runtime", "full_access"]} |
| key_prefix | 是 | string | {"minLength": 1} |
| scopes | 是 | #/$defs/stringList | {} |
| status | 是 | string | {"minLength": 1} |
| expires_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |
| last_used_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |
| created_at | 是 | string | {"format": "date-time"} |
| revoked_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |

### credentialCreated

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| id | 是 | string | {"minLength": 1} |
| agent_id | 是 | string | {"minLength": 1} |
| name | 是 | string | {"minLength": 1} |
| access | 是 | enum | {"enum": ["runtime", "full_access"]} |
| key_prefix | 是 | string | {"minLength": 1} |
| scopes | 是 | #/$defs/stringList | {} |
| status | 是 | string | {"minLength": 1} |
| expires_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |
| last_used_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |
| created_at | 是 | string | {"format": "date-time"} |
| revoked_at | 否/条件 | ['string', 'null'] | {"format": "date-time"} |
| api_key | 是 | string | {"minLength": 1} |

### legacyModelBindingInput

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| role | 是 | enum | {"enum": ["default", "router", "step", "response", "general_skill"]} |
| model_config_id | 是 | string | {"minLength": 1} |

### legacyModelsUpdate

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenant_id | 是 | string | {"minLength": 1} |
| bindings | 否/条件 | array | {"items": {"$ref": "#/$defs/legacyModelBindingInput"}} |

### legacyModelBindingRead

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| role | 是 | string | {} |
| model_config_id | 是 | string | {} |
| effective | 是 | boolean | {} |

## staffdeck-skill-card

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/staffdeck-skill-card.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| skill_id | 是 | string | {"minLength": 1} |
| name | 是 | string | {"minLength": 1} |
| version | 否/条件 | string | {"default": "1.0.0"} |
| business_domain | 否/条件 | ['string', 'null'] | {} |
| description | 否/条件 | string | {} |
| capability_scope | 否/条件 | enum | {"enum": ["general", "sop_specific"]} |
| step_timeout_seconds | 否/条件 | ['integer', 'null'] | {"minimum": 1, "maximum": 3600} |
| trigger_intents | 否/条件 | #/$defs/stringList | {} |
| user_utterance_examples | 否/条件 | #/$defs/stringList | {} |
| goal | 否/条件 | #/$defs/stringList | {} |
| required_info | 否/条件 | #/$defs/stringList | {} |
| slot_filling_policy | 否/条件 | object | {} |
| response_rules | 否/条件 | #/$defs/stringList | {} |
| nodes | 是 | array | {"minItems": 1, "items": {"$ref": "#/$defs/node"}} |
| edges | 否/条件 | array | {"items": {"$ref": "#/$defs/edge"}} |
| start_node_id | 是 | string | {"minLength": 1} |
| terminal_node_ids | 是 | array | {"minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}} |
| interruption_policy | 否/条件 | object | {} |

### capabilityRefs

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| general_skill_ids | 否/条件 | #/$defs/stringList | {} |
| tool_ids | 否/条件 | #/$defs/stringList | {} |
| knowledge_base_ids | 否/条件 | #/$defs/stringList | {} |
| required_general_skill_ids | 否/条件 | #/$defs/stringList | {} |
| required_tool_ids | 否/条件 | #/$defs/stringList | {} |
| required_knowledge_base_ids | 否/条件 | #/$defs/stringList | {} |

### node

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| node_id | 是 | string | {"minLength": 1} |
| type | 否/条件 | string | {} |
| name | 是 | string | {"minLength": 1} |
| instruction | 否/条件 | string | {} |
| optional | 否/条件 | boolean | {} |
| condition | 否/条件 | ['string', 'null'] | {} |
| expected_user_info | 否/条件 | #/$defs/stringList | {} |
| allowed_actions | 否/条件 | #/$defs/stringList | {} |
| knowledge_scope | 否/条件 | object | {} |
| capability_refs | 否/条件 | #/$defs/capabilityRefs | {} |
| retry_policy | 否/条件 | object | {} |
| metadata | 否/条件 | object | {} |
| sub_sop_id | 否/条件 | ['string', 'null'] | {} |
| assignee_user_id | 否/条件 | ['string', 'null'] | {} |

### edge

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| source_node_id | 是 | string | {"minLength": 1} |
| next_node_id | 是 | string | {"minLength": 1} |
| condition | 否/条件 | ['string', 'null'] | {} |
| priority | 否/条件 | integer | {} |
| label | 否/条件 | ['string', 'null'] | {} |

## subject

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/subject.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| subjectId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| naturalPersonId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| subjectType | 是 | enum | {"enum": ["HUMAN_USER", "AGENT_EXECUTION_PRINCIPAL", "CHANNEL_PRINCIPAL", "SERVICE_ACCOUNT", "SYSTEM_JOB", "SCHEDULER_PRINCIPAL", "RUNTIME_WORKER", "MCP_CONTROL_SERVICE", "CREDENTIAL_ROTATION_SERVICE", "DELEGATION_CASCADE_SERVICE"]} |
| authzEpoch | 否/条件 | integer | {"minimum": 0} |
| active | 否/条件 | boolean | {} |

## tenant-membership

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/tenant-membership.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| membershipId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| userId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| status | 是 | enum | {"enum": ["invited", "pending_activation", "active", "suspended", "disabled", "expired", "cancelled"]} |
| sourceType | 是 | enum | {"enum": ["LOCAL", "IDP", "SCIM", "HRIS"]} |
| sourceRef | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| validFrom | 是 | string | {"format": "date-time"} |
| validUntil | 否/条件 | string | {"format": "date-time"} |
| authzEpoch | 是 | integer | {"minimum": 0} |
| primaryOrgUnitId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| revision | 是 | integer | {"minimum": 1} |

## tenant

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/tenant.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| name | 是 | string | {"minLength": 1, "maxLength": 120} |
| status | 是 | enum | {"enum": ["draft", "active", "suspended", "closing", "closed"]} |
| ownerUserId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| defaultLocale | 是 | 组合定义 | {"pattern": "^[a-z]{2}(?:-[A-Z]{2})?$"} |
| defaultTimezone | 是 | string | {"pattern": "^[A-Za-z_+-]+(?:/[A-Za-z0-9_+-]+)+$"} |
| dataRegion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| authenticationPolicyId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| createdAt | 是 | string | {"format": "date-time"} |
| activatedAt | 否/条件 | string | {"format": "date-time"} |
| suspendedAt | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## usage-record

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/usage-record.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| usageRecordId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workExecutionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workItemId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| attemptId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| agentRevision | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| model | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| toolRevision | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| usageType | 是 | enum | {"enum": ["TOKEN", "TOOL_CALL", "STORAGE", "EGRESS", "MODEL_TIME"]} |
| quantity | 是 | number | {"minimum": 0} |
| unit | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| estimatedCost | 是 | number | {"minimum": 0} |
| currency | 是 | string | {"pattern": "^[A-Z]{3}$"} |
| pricingVersion | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| occurredAt | 是 | string | {"format": "date-time"} |
| evidenceId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |

## user

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/user.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| userId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| displayName | 是 | string | {"minLength": 1, "maxLength": 120} |
| status | 是 | enum | {"enum": ["active", "locked", "disabled", "recovery_pending"]} |
| locale | 否/条件 | 组合定义 | {"pattern": "^[a-z]{2}(?:-[A-Z]{2})?$"} |
| timezone | 否/条件 | string | {"pattern": "^[A-Za-z_+-]+(?:/[A-Za-z0-9_+-]+)+$"} |
| primaryEmailMasked | 否/条件 | string | {"maxLength": 254} |
| createdAt | 是 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

## work-execution-history-item

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/work-execution-history-item.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| workExecutionHistoryItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workExecutionId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sopId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sopRevision | 是 | integer | {"minimum": 1} |
| status | 是 | enum | {"enum": ["created", "running", "awaiting_human", "awaiting_approval", "completed", "failed", "cancelled", "reconciling"]} |
| startedAt | 是 | string | {"format": "date-time"} |
| completedAt | 否/条件 | string | {"format": "date-time"} |
| correlationDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| responsibleMembershipId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| evidenceBundleId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |

## work-thread-entry

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/work-thread-entry.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| workThreadEntryId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workThreadId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| sequence | 是 | integer | {"minimum": 1} |
| entryType | 是 | enum | {"enum": ["MESSAGE", "ARTIFACT", "CITATION", "DECISION", "TOOL_RECEIPT", "HUMAN_CORRECTION", "AGENT_CANDIDATE", "UNRESOLVED_ITEM"]} |
| actorSubjectType | 是 | enum | {"enum": ["HUMAN", "AGENT", "SYSTEM"]} |
| actorSubjectId | 否/条件 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| contentDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| contentRef | 否/条件 | string | {"minLength": 1} |
| classification | 是 | enum | {"enum": ["C0", "C1", "C2", "C3", "C4"]} |
| purpose | 是 | string | {"minLength": 1} |
| authzSnapshotDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| occurredAt | 是 | string | {"format": "date-time"} |

## work-thread

[当前源合同](</Users/mac/Documents/ChatGPT/ZKER- staff/contracts/jsonschema/work-thread.schema.json:1>)

| 字段 | 必填 | 类型/引用 | 约束/默认 |
|---|---|---|---|
| workThreadId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| tenantId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| workItemId | 是 | string | {"pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"} |
| participantManifestDigest | 是 | string | {"pattern": "^sha256:[0-9a-f]{64}$"} |
| lastSequence | 是 | integer | {"minimum": 0} |
| createdAt | 是 | string | {"format": "date-time"} |
| closedAt | 否/条件 | string | {"format": "date-time"} |
| revision | 是 | integer | {"minimum": 1} |

