# 产品需求对齐（优先于编码）

> **状态**：CODING_PAUSED — 先对齐需求，再写/扩代码。  
> **事实源**：本机 `/Users/mac/Documents/OA数字员工协作平台`（设计主链 00–60 + contracts）。  
> **Git 仓**：仅 `https://github.com/wilson323/ZKER-staff-1.0.git`。  
> **标注**：凡「待本机材料对齐后迁入」均非正式定稿。

## 1. 对齐目标

在继续实现前，必须与本机设计主链对齐并书面确认：

1. **产品是什么**（边界、非目标）
2. **给谁用**（角色与主场景）
3. **M0/M1 最小可交付**（可验收行为，不是技术栈口号）
4. **底座与开源选型**（可商用宽松协议；Owner 签署或明确「仅授权 M0 实验」）
5. **契约来源**（contracts 字段与 API 以本机为准迁入）

## 2. 已确认事实（来自既有梳理，非臆造）

| 项 | 结论 | 来源级别 |
|---|---|---|
| 产品名 | OA 数字员工协作平台 / ZKER-staff-1.0 Enterprise Digital Employee Platform | 仓库 README + 用户指令 |
| 工程仓 | 空仓起步的 `wilson323/ZKER-staff-1.0` | `git remote` 已核验 |
| 设计主链 | 本机 00–60 无跳号；含 contracts、开发体系、原型等 | 本机材料梳理结论 |
| 平台是否已创建 | **否**（无产品源码终态、底座曾为 `PENDING_SELECTION`、AC 多为 `NOT_RUN`） | 本机材料梳理结论 |
| 云端完整设计包 | **缺失/未落盘**；上传路径在本执行环境不可用 | 本执行环境检查 |
| 本机对齐 | 任务进行中（协调侧） | 用户补充约束 |
| 编码纪律 | 禁止 mock；AI 走 OpenAI 协议适配；优先可商用开源 | 用户规则 + NOTICE 策略 |

## 3. 待本机对齐的关键问题（阻塞编码深化）

### P0 — 必须回答后才能扩业务代码

1. **产品一句话定义**  
   - 数字员工「协作」具体指：任务编排 / 对话助理 / OA 流程代办 / 多员工会商 / 其他？
2. **首期用户与场景**  
   - 谁登录？完成哪 1～3 个主路径？成功标准是什么？
3. **数字员工对象模型**  
   - 身份、技能、工具、权限、记忆、审计字段以哪份 contracts 为准？
4. **与现有 OA/企业系统关系**  
   - 独立产品还是嵌入现有 OA？是否已有账号/组织主数据权威源？
5. **底座终选**  
   - Owner 是否签署？或是否书面「仅授权 M0 工程门禁实验」？
6. **视觉源**  
   - 是否声明「以 31 号 HTML 为唯一视觉源」，或另有 Figma/品牌包？

### P1 — 影响架构但不阻塞「需求文档定稿」

1. 部署形态（单机 / K8s / 私有化离线）
2. 模型供应商与数据出境约束
3. 多租户 / 组织隔离是否 M1 必含

## 4. 需求对齐工作流（与本机同步）

```text
本机事实源产出/更新
  → 云端 docs 仅摘录「已确认摘要 + 迁入清单」
  → Owner 确认 M0 范围与底座
  → 再按本机 47 号最小步骤编码
```

**禁止**：在 contracts / 00–60 未对齐前发明领域 API、假数据或不可商用底座。

## 5. M0 需求边界（建议稿，待 Owner 批）

> 下列为**建议对齐稿**，不是本机设计替代。

| 编号 | 需求 | 验收 | 备注 |
|---|---|---|---|
| R-M0-01 | 仓库可 install/build/test | `make ci` 通过 | 工程门禁 |
| R-M0-02 | 健康检查真实可用 | `GET /api/v1/health` → ok | 无业务伪造 |
| R-M0-03 | 数字员工列表契约未迁入前为空登记 | `total=0` | 禁止 mock 员工 |
| R-M0-04 | AI 未配置密钥时真实失败 | probe/createClient 行为可测 | OpenAI 协议 |
| R-M0-05 | 许可证策略可商用 | NOTICE 列出 MIT/Apache 组件 | 禁 AGPL 主路径 |
| R-M0-06 | 设计/contracts 迁入点预留 | docs 标明待迁入 | **待本机材料对齐后迁入** |

## 6. 非目标（M0）

- 不实现完整数字员工运行时 / 编排引擎
- 不编造 00–60 未给出的业务模块
- 不切换到其他 Git 仓库
- 不引入需付费闭源 SDK 作为硬依赖（除非 Owner 批准）

## 7. 开源商用约束（需求层）

选型必须满足：

1. **宽松协议**（MIT / Apache-2.0 / BSD）可进入闭源商用发行
2. **成熟维护**（官方脚手架/广泛生产采用）
3. **可替换**（底座未终选前，目录级可替换，不绑死领域模型）

具体组件名单以 `NOTICE.md` 为准；业务能力仍 **待本机材料对齐后迁入**。

## 8. 当前决策

| 决策 | 内容 |
|---|---|
| 编码 | **暂停深化**；已有脚手架仅作工程占位，不视为需求已落地 |
| 下一步 | 等待本机对齐结果 → 更新本文件「已确认」表 → Owner 批 M0 范围 → 再编码 |
| 文档 | 以本文件 + `docs/oa-platform-plan.md` 为云端对齐入口 |

## 9. 回传清单（供本机对齐任务）

请本机对齐完成后回传/迁入：

1. 产品一句话定义 + 非目标  
2. 角色 × 主场景 × 成功标准  
3. contracts 包（或指向本机路径的权威摘要）  
4. 底座终选签署或「仅 M0 实验」书面授权  
5. 47 号最小步骤中与工程仓相关的条目摘录  
