--- upstream/ima_api.cjs
+++ installed/ima_api.cjs
@@ -87,6 +87,40 @@
   fs.writeFileSync(lastCheckFile, today, 'utf8');
 }
 
+function compareSemver(left, right) {
+  const parse = (value) => {
+    const match = String(value || '').trim().match(/^v?(\d+)\.(\d+)\.(\d+)(?:-([0-9A-Za-z.-]+))?$/);
+    if (!match) return null;
+    return {
+      core: match.slice(1, 4).map(Number),
+      prerelease: match[4] ? match[4].split('.') : [],
+    };
+  };
+  const a = parse(left);
+  const b = parse(right);
+  if (!a || !b) return null;
+  for (let index = 0; index < 3; index += 1) {
+    if (a.core[index] !== b.core[index]) return a.core[index] > b.core[index] ? 1 : -1;
+  }
+  if (a.prerelease.length === 0 && b.prerelease.length === 0) return 0;
+  if (a.prerelease.length === 0) return 1;
+  if (b.prerelease.length === 0) return -1;
+  const length = Math.max(a.prerelease.length, b.prerelease.length);
+  for (let index = 0; index < length; index += 1) {
+    const av = a.prerelease[index];
+    const bv = b.prerelease[index];
+    if (av === undefined) return -1;
+    if (bv === undefined) return 1;
+    if (av === bv) continue;
+    const an = /^\d+$/.test(av);
+    const bn = /^\d+$/.test(bv);
+    if (an && bn) return Number(av) > Number(bv) ? 1 : -1;
+    if (an !== bn) return an ? -1 : 1;
+    return av > bv ? 1 : -1;
+  }
+  return 0;
+}
+
 async function checkSkillUpdateIfNeeded(requestOptions) {
   const { forceCheck, skillVersion, lastCheckFile } = requestOptions;
 
@@ -117,7 +151,11 @@
   const latestVersion = (updateResp.data && updateResp.data.latest_version) || '';
   const releaseDesc = (updateResp.data && updateResp.data.release_desc) || '';
   const instruction = (updateResp.data && updateResp.data.instruction) || '';
-  if (latestVersion && latestVersion !== skillVersion) {
+  const versionOrder = compareSemver(latestVersion, skillVersion);
+  // Only a strictly newer valid semantic version may block the business request.
+  // A stale server catalog (for example latest=1.1.8 while current=1.1.9)
+  // must never force a downgrade or prevent read/write operations.
+  if (latestVersion && versionOrder !== null && versionOrder > 0) {
     const updateContext = {
       current_version: skillVersion,
       latest_version: latestVersion,
@@ -221,6 +259,7 @@
 
 module.exports = {
   imaApi,
+  compareSemver,
   ERR_PROGRAMMATIC,
   ERR_UPDATE_AVAILABLE,
 };
